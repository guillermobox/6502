# 6502 emulator in C

This is just a basic 6502 emulator in C. Compile with 'make' in the root folder.

Afterwards, if you have the cc65 compiler suite installed, and pytest, you can
run make in the tests folders to run some tests, and also in the example folder
to run an example.
